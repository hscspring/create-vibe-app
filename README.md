# create-vibe-app

A CLI tool to create Vibe Coding projects.

## Installation

```bash
pip install create-vibe-app
```

## Usage

```bash
create-vibe-app my-new-project
```

This will create a new directory `my-new-project` (or use the current one) and populate it with the Vibe Coding structure.
